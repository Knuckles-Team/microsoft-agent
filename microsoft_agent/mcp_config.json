{
  "mcpServers": {
    "microsoft": {
      "command": "microsoft-mcp",
      "args": ["--transport", "stdio"],
      "env": {
        "MICROSOFT_URL": "${MICROSOFT_URL:-http://localhost:8080}",
        "MICROSOFT_TOKEN": "${MICROSOFT_TOKEN}"
      }
    }
  }
}
